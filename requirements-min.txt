# Minimal runtime — text-only delivery, no PDF stack. For constrained hosts (e.g. Android/Termux).
# Pair with REPORT_PDF=0 in .env (the bot then sends chunked text instead of a PDF).
#
# Termux note: PyPI numpy/pandas wheels are glibc/manylinux and DON'T run on Termux (bionic libc).
# Install those from Termux first:  pkg install python-numpy  (+ pandas via pip or tur-repo),
# then `pip install` the rest below. See DEPLOY.md → "Run on an Android phone (Termux)".
openai>=1.30
yfinance>=0.2.50
pandas>=2.0
numpy>=1.24
requests>=2.31
python-telegram-bot>=20.0
python-dotenv>=1.0
