# Core
# (No `openai` SDK — we use llm.py, a requests-based OpenAI-compatible client, to avoid the
#  Rust-built jiter/pydantic-core deps that don't build on Termux/Android.)
yfinance>=0.2.50          # needs Sector/Industry classes for tool_sector
pandas>=2.0
numpy>=1.24
requests>=2.31
python-telegram-bot>=20.0
python-dotenv>=1.0

# PDF delivery (bot sends the report as a dated PDF; falls back to text if missing)
markdown>=3.5
xhtml2pdf>=0.2.16

# Optional (portfolio intake / docx reports — not used by stock-research mode)
# openpyxl>=3.1
# rapidfuzz>=3.0
# python-docx>=1.1
